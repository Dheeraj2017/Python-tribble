#!/usr/bin/python
tup = ('physics', 'chemistry', 1997, 2000);
print (tup);
del tup;
print ("After deleting tup : ")
print (tup);
# in the above code it is showing error because due to del function above delete the tup variable 
# so it is not showing the desired Output
# now it is quiet good because it will save the memory